-  name: Installing nginx and starting
   hosts: localhost
   become: true
   tasks:
     - name: Installation
       apt:
         name: nginx
         state: present
         update_cache: true
     
     - name: Start and Enabled
       service:
         name: nginx
         enabled: true
         state: started  
